#include <stdio.h>

int main(){
    FILE *fp = fopen ("testdata.in", "r");
    int T;
    fscanf(fp, "%d", &T);

    for(int i=0;i<T;i++){
        char X[100];
        int N;        
        fscanf(fp,"%s", X);
        fscanf(fp,"%d", %N);
        for(int j=0;j<N;j++){
            
        }
    }
}